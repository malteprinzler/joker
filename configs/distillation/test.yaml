name: example_data_0_test
tag: example_data  # group of experiments
exp_root_dir: "experiments/distillation"
seed: 0
debug: False
mode: test
resume: "experiments/distillation/example_data/example_data_0/ckpts/last.ckpt"

data_type: "joker-datamodule"
data:
  root: "data/DISTILLATION_EXAMPLE_DATA/000/sequences/HORIZONTAL_SWEEP/frame_00000"
  caption_dir: "blip_captions"

system_type: "imagedream-system"
system:
  update_steps_per_target_update: 130
  geometry_type: "implicit-volume"
  geometry:
    radius: 0.25
    normal_type: null
    density_bias: "blob_magic3d"
    density_activation: softplus
    density_blob_scale: 10.
    density_blob_std: 0.125

    pos_encoding_config:
      otype: HashGrid
      n_levels: 16
      n_features_per_level: 2
      log2_hashmap_size: 19
      base_resolution: 16
      per_level_scale: 1.447269237440378 # max resolution 4096

  material_type: "no-material"
  material:
    n_output_dims: 3
    color_activation: sigmoid

  background_type: "neural-environment-map-background"
  background:
    color_activation: sigmoid
    random_aug: false
    share_aug_bg: false

  renderer_type: "nerf-volume-renderer"
  renderer:
    radius: ${...system.geometry.radius}
    num_samples_per_ray: 200
